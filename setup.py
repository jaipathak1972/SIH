from setuptools import setup, find_packages

setup(
    name='speechcare',
    version='1.0',
    packages=find_packages(),
    install_requires=[
        # List your package dependencies here
    ],
)
